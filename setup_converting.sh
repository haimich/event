# Add repo (Ubuntu)
sudo add-apt-repository ppa:mc3man/trusty-media
sudo apt-get update
#sudo apt-get dist-upgrade => skipping, takes too much time

# Add repo (Debian)
# => see http://www.deb-multimedia.org/

# ffmpeg
sudo apt-get install ffmpeg